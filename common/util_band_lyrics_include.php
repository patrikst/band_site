<?php

// ============================================
//	PRINT EDIT/ADD FORM - LYRICS
// ============================================

function ubd_ly_print_form($debug, $bandID, $edit, $lyricID)
{
	if ($debug) up_error("ubd_ly_print_form: ENTER");

	if ($debug) up_error("ubd_ly_print_form: EXIT");
}


// ============================================
//	VIEW - LYRICS
// ============================================

function ubd_ly_view($debug, $bandID, $lyricID) // If lyricID == "" => View All
{
	if ($debug) up_error("ubd_ly_view: ENTER");

	if ($debug) up_error("ubd_ly_view: EXIT");
}

// ============================================
//	ADD - LYRICS
// ============================================

function ubd_ly_add($debug, $bandID, $title, $artist  )
{
	if ($debug) up_error("ubd_ly_add: ENTER");

	if ($debug) up_error("ubd_ly_add: EXIT");
}


// ============================================
//	UPDATE - LYRICS
// ============================================

function ubd_ly_update( $debug, $bandID, $lyricID, $date, $time, $notes, $location )	
{
	if ($debug) up_error("ubd_ly_update: ENTER");

	if ($debug) up_error("ubd_ly_update: EXIT");
}

// ============================================
//	DELETE - LYRICS
// ============================================

function ubd_ly_delete($debug, $bandID, $lyricID)
{
	if ($debug) up_error("ubd_ly_delete: ENTER");

	if ($debug) up_error("ubd_ly_delete: EXIT");
}	

?>